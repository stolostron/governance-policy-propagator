name: Framework

on:
  pull_request:
    branches:
      - main
      - release-[0-9]+.[0-9]+

jobs:
  fw-kind-tests:
    name: KinD
    uses: stolostron/governance-policy-framework/.github/workflows/kind.yml@499dd1258fa26c15beafc6630f8bf75ec6db6c83 # main
    with:
      hub_only_component: 'true'
